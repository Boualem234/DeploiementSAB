<UserControl x:Class="GameOn.Views.AdminView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GameOn.Views"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             Background="White"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="65"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <!-- menu -->
        <Border Margin="5,5,5,0" 
                Grid.Row="0">
            <Menu materialDesign:MenuAssist.TopLevelMenuItemHeight="25"
                   Margin="100,0,50,0"
                  VerticalAlignment="Bottom">
                <MenuItem 
                Background="MediumPurple"
                Foreground="White"
                Header="Employé" Click="MenuEmploye_Click"/>
            </Menu>
        </Border>
        
        <!-- espace pour les tableau de emp + departement -->
        <Border Grid.Row="1" Margin="50, 0,50,20"
                Background="Aqua">
            <local:AdminEmployeView x:Name="AdminEmployeView" Visibility="Visible"/>
        </Border>
    </Grid>
</UserControl>
