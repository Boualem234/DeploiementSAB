<Window x:Class="GameOn.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GameOn.Views"
        mc:Ignorable="d"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Style="{StaticResource MaterialDesignWindow}"
        Title="Gestionnaire de jeu" 
        Height="700" Width="1400"
        MinHeight="700" MinWidth="1400"
        Icon="/img/logoGameOn2.png">

    <Grid x:Name="MainGrid">
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <TextBlock x:Name="HeaderTextBlock" VerticalAlignment="Center" HorizontalAlignment="Center" Text=""/>
        </Grid>

        <Grid Grid.Row="1">
            <local:LoginView x:Name="loginV" Visibility="Visible"/>
        </Grid>
    </Grid>


</Window>